Run `npm run full-check` and fix any failures until it passes.

1. Execute `npm run full-check` in the project root.
2. If it fails: fix the reported errors and run again until it passes.
3. Exception: if failures are clearly in code you did not modify (pre-existing), report them to the user instead of iterating indefinitely.
4. Do not consider the task done until full-check passes (or you've reported pre-existing failures).
