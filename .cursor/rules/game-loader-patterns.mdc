---
description: Patterns for game definitions and game loading system
globs:
  - src/game-loader/**/*.ts
  - public/games/**/*.json
---

# Game Loader Patterns

## Game Definition Structure

Each game lives in `public/games/[game-name]/config.json`:

```json
{
  "metadata": {
    "id": "game-id",
    "name": "Display Name",
    "minPlayers": 2,
    "maxPlayers": 4
  },
  "rules": {
    "objective": "Win condition",
    "summary": "Optional short summary for NARRATE"
  },
  "squares": {
    "0": { "type": "empty", "next": { "1": ["left"], "15": ["right"] } },
    "10": { "type": "animal", "name": "Wolf", "power": 3 },
    "45": { "type": "portal", "destination": 82 },
    "186": { "type": "special", "effect": "magicDoorCheck", "target": 6 },
    "196": { "type": "special", "effect": "win" }
  },
  "soundEffects": {
    "roll": "/games/[game-name]/sounds/roll.mp3",
    "win": "/games/[game-name]/sounds/win.mp3"
  }
}
```

## Config formats

**New (squares-only):** Define `squares` at top level. Board (winPosition, moves, magicDoor), game state, and players are derived at load. See `deriveBoardFromSquares` in game-loader.

**Legacy:** Define `initialState` with full game/players/board. Still supported for backward compatibility.

## Decision Points

Decision points (forks) are inferred from board squares: any square with `next` mapping to multiple targets becomes a decision point.

## How Games Are Loaded

1. `GameLoader.loadGame(gameName)` fetches config JSON
2. Resolves initialState (from config.initialState or derived from config.squares)
3. Hydrates board topology (fills empty squares, default next/prev)
4. Preloads sound effects via WebAudio
5. Returns game module to orchestrator

## LLM Integration

Game rules are passed to LLM via system prompt:

- `rules` section becomes LLM context
- LLM understands game mechanics
- LLM translates player actions to primitives
- Orchestrator executes primitives deterministically

## Current Scope

MVP is Kalimba-only. `BoardEffectsHandler` contains Kalimba board logic (board.moves, magic door, square effects). Game rules come from config; orchestrator executes primitives deterministically.

## Sound Effects

Sound effects are preloaded at game start:

```typescript
const soundEffects = await gameLoader.loadSoundEffects(gameModule);
// Returns Map<string, AudioBuffer>
```

Used by SpeechService for:

- Dice rolls
- Win conditions
- Special events
- Ambient sounds (future)
